Script validates input file argument + readability

Uses regex: ^[A-Za-z0-9]+@[A-Za-z]+\.com$ to classify valid vs invalid

Writes valid emails to valid.txt, invalid to invalid.txt

Uses sort | uniq to remove duplicates from valid list
